{% extends 'app' %}

{% block body %}
  <div class="max-w-2xl mx-auto p-6 mb-10">
    <h1 class="font-medium text-pink-900 text-3xl/snug">{{ page.title }}</h1>
      <div class="{{ site.proseClasses }}">
        {% block content %}{% endblock %}
      </div>
  </div>
{% endblock %}
